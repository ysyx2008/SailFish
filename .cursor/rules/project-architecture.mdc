---
description: 项目架构全景，新会话必读；重大功能更新后必须同步更新本文件
alwaysApply: true
---

# SailFish 项目架构

> **每次新会话开始时**应先阅读本文件以快速了解项目。
> **完成重大功能新增或架构变更后**，必须更新本文件中相关的章节，保持信息准确。

## 概览

SailFish（旗鱼）是一个私人 AI Agent 桌面应用（代码仓库名 SFTerminal 为历史遗留）。用户用自然语言描述需求，AI 自主规划并执行任务。支持本地和远程 (SSH) 环境操作，可通过钉钉/飞书/企业微信/Slack/Telegram 远程访问 Agent。

| 层 | 技术 | 说明 |
|---|---|---|
| 桌面壳 | Electron 37 | 窗口管理、系统集成、IPC |
| 前端 | Vue 3 + Pinia + xterm.js | UI、终端渲染、状态管理 |
| 后端服务 | TypeScript (Node.js) | 26 个服务，业务逻辑核心 |
| CLI | Node.js + tsx | 纯命令行模式，复用后端服务 |
| AI | Transformers.js + LanceDB | 本地向量嵌入、知识库 |
| 构建 | Vite + electron-builder | 多平台打包（macOS/Win/Linux） |

## 目录结构

```
SFTerminal/
├── electron/                    # Electron 主进程 + 后端服务
│   ├── main.ts                  # 主进程入口（窗口、IPC、服务初始化）
│   ├── preload.ts               # 预加载脚本（IPC 桥接）
│   ├── cli/                     # CLI 模式入口
│   │   ├── main.js              # CLI 启动器（JS，注册 electron-shim）
│   │   ├── electron-shim.js     # Electron API 模拟层
│   │   ├── index.ts             # CLI 命令实现
│   │   └── test-cli.sh          # CLI 自动化回归测试
│   └── services/                # 后端服务
│       ├── ai.service.ts        # AI 对话（多 provider 支持）
│       ├── config.service.ts    # 配置管理（electron-store）
│       ├── pty.service.ts       # 本地终端（node-pty）
│       ├── ssh.service.ts       # SSH 连接管理
│       ├── sftp.service.ts      # SFTP 文件传输
│       ├── unified-terminal.service.ts # 统一终端服务
│       ├── terminal-state.service.ts   # 终端状态管理
│       ├── host-profile.service.ts     # 主机画像
│       ├── history.service.ts   # 对话/Agent 历史（L3 对话记录）
│       ├── scheduler.service.ts # 定时任务（已整合到 Watch）
│       ├── mcp.service.ts       # MCP 服务器管理
│       ├── local-fs.service.ts  # 本地文件系统
│       ├── document-parser.service.ts  # 文档解析
│       ├── file-search.service.ts      # 文件搜索
│       ├── user-skill.service.ts       # 用户技能
│       ├── skill-market.service.ts     # 技能市场
│       ├── gateway.service.ts   # 网关服务
│       ├── credential.service.ts # 凭证存储
│       ├── remote-chat.service.ts      # 远程对话服务
│       ├── screen-content.service.ts   # 屏幕内容服务
│       ├── command-executor.service.ts # 命令执行服务
│       ├── menu.service.ts      # 菜单服务
│       ├── ai-debug.service.ts  # AI 调试服务
│       ├── xshell-import.service.ts    # Xshell 导入
│       ├── agent/               # AI Agent 子系统
│       │   ├── agent.ts         # Agent 抽象基类（ReAct 循环、会话追踪、持久化、观察提取）
│       │   ├── index.ts         # AgentService（Agent 实例管理，按 ptyId 复用）
│       │   ├── sailfish.ts      # 旗鱼 Agent 实现
│       │   ├── orchestrator.ts  # 编排器（多 Agent 协调）
│       │   ├── tool-executor.ts # 工具执行器
│       │   ├── tools.ts         # 工具定义（Function Calling）
│       │   ├── tools/           # 工具实现（command/file/knowledge/plan/memory/terminal/context/exec）
│       │   ├── skills/          # 技能系统（excel/email/browser/word/watch/scheduler/calendar/terminal/skill-creator）
│       │   ├── prompt-builder.ts # 系统提示构建
│       │   ├── context-builder.ts # 上下文管理（L1 任务历史压缩）
│       │   ├── task-memory.ts   # 任务记忆存储（L1）
│       │   ├── risk-assessor.ts # 风险评估
│       │   └── i18n.ts          # Agent 国际化
│       ├── watch/               # Watch 感知层
│       ├── sensor/              # 传感器系统（heartbeat/file-watch/calendar/email）
│       ├── terminal-awareness/  # 终端感知（进程监控）
│       ├── knowledge/           # 知识库（L2 知识文档，向量存储 + 嵌入 + BM25）
│       ├── im/                  # IM 集成（钉钉/飞书/企业微信/Slack/Telegram）
│       ├── speech/              # 语音识别
│       └── __tests__/           # 服务单元测试
├── src/                         # 前端（Vue 3）
│   ├── views/                   # 页面视图
│   ├── components/              # 组件
│   │   ├── Settings/            # 设置页组件
│   │   ├── FileManager/         # 文件管理器
│   │   ├── FileExplorer/        # 文件浏览器
│   │   └── ...
│   ├── stores/                  # Pinia 状态管理
│   │   ├── terminal.ts          # 终端状态、标签页、Agent 状态
│   │   └── config.ts            # 配置、AI profiles、SSH 会话
│   ├── composables/             # 组合式函数
│   ├── i18n/                    # 国际化（中/英）
│   └── themes/                  # 终端主题
├── resources/                   # 打包资源（嵌入模型、语音模型等）
├── scripts/                     # 构建/模型下载脚本
├── docs/                        # 文档
├── website/                     # 官网
└── package.json                 # 依赖 & 脚本
```

## 关键架构模式

### 前后端通信
- Electron IPC：`ipcMain.handle` ↔ `ipcRenderer.invoke`
- 前端通过 `preload.ts` 暴露的 API 调用后端服务
- `main.ts` 中注册所有 IPC handler，大多是对服务方法的薄封装

### CLI 模式（纯 Node.js 运行后端）
- **入口**：`electron/cli/main.js`（纯 JS）
- **原理**：通过 `Module._resolveFilename` 拦截 `require('electron')`，替换为 `electron-shim.js`
- **特点**：所有后端服务无需修改即可在 CLI 下运行
- **配置隔离**：CLI 使用独立配置文件 `qiyu-terminal-config-cli`，避免与 Electron 加密配置冲突
- **用法**：`npm run sft -- <command> [options]`
- **测试**：`bash electron/cli/test-cli.sh`（33 个命令全覆盖，含 Watch & Sensor）

### 服务实例化
- 服务在 `main.ts` 中直接实例化，部分使用工厂函数（如 `getKnowledgeService()`）
- CLI 中通过懒加载单例模式按需创建服务实例
- 部分服务支持构造器注入（如 `AgentService` 注入 AI、PTY、HostProfile 等）

### AI Agent 架构
- **ReAct 循环**：思考 → 工具调用 → 观察结果 → 循环
- **工具系统**：内置工具 + 技能系统（按需加载）+ MCP 工具
- **风险评估**：safe / moderate / dangerous / blocked 四级
- **执行模式**：strict（全确认）/ relaxed（危险确认）/ free（全自动）
- **Agent Workspace**：`{userData}/agent-workspace/`，Agent 私有数据目录，文件读写自动跳过确认
- **三级记忆架构**：见下方独立章节
- **上下文管理**：`ContextBuilder` 根据 token 预算动态选择压缩级别

### Agent 会话与持久化
- **后端为唯一数据源**：Steps（含 `user_task`/`final_result`）和 API messages 均由后端 Agent 生成、管理和持久化，前端仅负责渲染
- **会话追踪**：Agent 实例按 `ptyId` 复用（`AgentService.agents` Map），会话状态（sessionId、累积 steps/messages）跨多次 `run` 持久化
- **完整消息记录**：每次 `run` 通过 `taskMessageLog`（append-only）记录完整 API 对话，与可压缩的 `run.messages`（工作窗口）分离
- **后端直接存盘**：`finalizeRun` 时 Agent 直接调 `HistoryService.saveAgentRecord` 保存 `AgentRecord`（含 `steps` + `messages`），前端不参与保存
- **增量检查点**：每完成一轮工具调用后自动写盘，防止程序意外退出丢失记录
- **跨会话恢复**：App 重启后，前端从 HistoryService 加载历史步骤用于 UI 渲染，只传 `sessionId` 给后端；后端通过 `HistoryService.getAgentRecordById` 自行加载完整 messages 和 steps 恢复 TaskMemory
- **生命周期**：终端关闭时 `cleanupAgent` 销毁 Agent 实例；"清空对话"时 `resetSession` 只重置会话状态和 TaskMemory，保留实例

### 关切系统（Watch / Sensor Loop）
- **关切（Watch）**：Agent 的"关注点"配置，定义触发条件、执行 prompt、输出渠道
- **Agent 驱动**：关切主要由 Agent 通过 `watch_*` 工具创建和管理，UI 面板为只读仪表盘
- **触发器（8 种）**：cron / interval / heartbeat / webhook / manual / file_change / calendar / email
- **传感器（4 种）**：HeartbeatSensor / FileWatchSensor / CalendarSensor / EmailSensor
- **Webhook**：Gateway 的 `POST /hooks/:token` 端点，外部系统可触发 Watch
- **Pre-check**：执行前让 AI 自主判断"该不该做"（Agency of Omission）
- **有状态工作流**：Watch 自身状态 + 跨 Watch 共享状态，Agent 可通过 `STATE_UPDATE` / `SHARED_UPDATE` 指令自动更新
- **模板系统**：8 个内置模板（晨间简报 / 日历提醒 / 邮件分类 / 日志监控 / 部署通知 / 服务器巡检 / 文件监控 / 工作总结）
- **串行执行**：所有事件通过 EventBus 排队，Watch 依次处理，不并发
- **Scheduler 废弃**：旧版定时任务已整合到关切系统，启动时自动迁移数据，`schedule_*` 工具标记为废弃
- **详细设计**：见 `docs/sensor-loop-architecture.md`

### 三级记忆架构

Agent 的记忆分三层，模拟人类记忆的衰减梯度，三层协同在有限上下文窗口内模拟无限记忆：

**L1 — 任务记忆（工作记忆）**
- 当前会话中最近几个任务的对话记录，直接作为 messages 注入上下文
- 5 级渐进式压缩（L0 完整对话 → L4 一句话），越近期越完整，在固定 token 预算内最大化信息保留
- 类比：刚做完的事，记得每句话

**L2 — 知识文档（长期事实记忆）**
- 按上下文维度（contextId）组织的持久化知识，每次对话时自动注入 system prompt
- contextId 根据场景不同：终端模式 = hostId（主机档案），无终端模式 = userId（用户个人档案）
- 设计目标：每个 contextId 对应一份结构化知识文档，固定 token 预算，整份注入，少而精
- 核心原则：只保留高频复用的核心事实，每条信息都值得花 token 自动注入
- 类比：记住的客观知识，不需要回忆具体经历就能想起来

**L3 — 对话记录（情景记忆）**
- 完整的历史对话记录（messages、steps、工具调用细节），永久保存
- Agent 通过搜索工具主动检索，按需加载，不占日常 token
- 与 L2 互补：L2 提供事实（"nginx 监听 8080"），L3 提供经验（"上次怎么排查的"）
- 有了 L3 兜底，L2 不需要"什么都记"，可以收紧写入标准
- 类比：需要"想一想"才能回忆的完整经历

### 日志系统

- **库**：`electron-log` v5.x，同时输出 console + 文件
- **文件路径**：`{userData}/logs/{date}.log`（如 macOS `~/Library/Application Support/SailFish/logs/2026-02-23.log`）
- **按日期分文件**：每天一个 `.log` 文件，自动保留 30 天
- **统一接口**：`createLogger(tag)` 返回带 scope 的 Logger（`log.info/warn/error/debug`）
- **初始化**：`initLogging()` 在 `main.ts` 和 CLI `index.ts` 启动时调用
- **级别控制**：通过设置页面或 `config:setLogLevel` IPC 动态调整
- **源码**：`electron/utils/logger.ts`

## 构建与发布

- **开发**：`npm run dev`（Vite dev server + Electron）
- **构建**：`npm run build`（Vite build → electron-builder）
- **平台**：macOS (universal)、Windows (NSIS)、Linux (AppImage/deb)
- **自动更新**：GitHub Releases + electron-updater
- **模型**：构建前需 `npm run download:models` 下载嵌入/语音模型

## 常用命令

```bash
npm run dev           # 开发模式
npm run build         # 完整构建
npm run sft -- --help # CLI 帮助
npm run test          # 运行测试
bash electron/cli/test-cli.sh  # CLI 回归测试
```
