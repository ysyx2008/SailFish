---
description: Spec 驱动开发规范，确保 AI 在改动模块前先理解契约，改动后保持 spec 同步
alwaysApply: true
---

# Spec 驱动开发

## SPEC.md 的作用

每个核心模块/服务目录下可能存在 `SPEC.md`（或 `*_SPEC.md`），描述该模块的契约：职责、公开 API、依赖、关键约束。它是 AI 理解模块的首要入口，比直接阅读源码更高效。

## 改动前

改动某个模块（`electron/services/` 下的服务或子系统）之前：
1. 先检查该目录下是否有 `SPEC.md`
2. 有则**先读 SPEC.md**，理解模块契约后再决定是否需要阅读具体源码
3. 没有则按常规方式阅读源码

## 改动后

完成代码改动后，检查是否需要同步更新 SPEC.md：
- **需要更新的情况**：新增/删除公开方法、改变模块职责、新增依赖、修改关键约束
- **不需要更新的情况**：Bug 修复、内部实现重构（不影响契约）、调参数

## 新增模块时

新增服务或子系统时，创建对应的 SPEC.md。内容保持精简（50-150 行），包含：
- 职责（1-2 句话）
- 公开 API（方法签名 + 简述）
- 文件结构（多文件模块）
- 依赖
- 关键约束

## 原则

- **指向而非复制**：不在 spec 里重复类型定义，用"见 `types.ts`"引用
- **重契约轻细节**：只写不容易变的东西（职责、约束），容易变的让代码自描述
- **宁可少写不要写错**：过期的 spec 比没有 spec 更有害
