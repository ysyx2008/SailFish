---
description: 完成新功能开发或较大修改后，使用本机 Claude CLI 进行代码审查
alwaysApply: true
---

# Claude CLI Code Review

当完成新功能开发或较大规模的代码修改后，使用本机已安装的 `claude` CLI 对改动进行一次代码审查。

## 流程

1. 通过 `git diff` 获取本次改动内容
2. 调用 `claude -p` 将 diff 和相关文件内容传入，请求其进行 code review
3. 审查关注点：代码健壮性、安全性、性能、可读性与可维护性
4. 根据审查结果自行判断是否需要采纳和修复，无需用户确认，修复后简要告知用户改了什么及原因
